# Node

Node is a processing unit within the ADCS system that represents either a provider, an adapter. A node should have these properties:

- `id`: unique id of the node, generated by the system, you dont need to specify
- `input`: input of the node
- `input_method`
  - for provider: the method name of the provider
  - for adapter: You should leave this empty
- `output`: output of the node
  - You should standardize the naming of these outputs. Eg. OP1 for Output of provider 1
  - These outputs can used as inputs for other nodes in the GraphFlow

# Example:

Provider Node P1:

```json
{
  "id": "P1",
  "input": ["IR.coinSymbol"],
  "input_method": "getPrice",
  "output": "OP1"
}
```

Adapter Node A1:

```json
{
  "id": "A1",
  "input": ["OP1"],
  "input_method": "",
  "output": "OA1"
}
```
